export default {
  run (commandString) {
    return new Promise((resolve, reject) => {
      // Create temp file with hosts content
      // Create temp file with bat script and 'ECHO' command with proper host path
      // Run bat file, resolve exit code
    })
  }
}
